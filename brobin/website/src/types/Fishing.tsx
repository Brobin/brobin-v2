export type FishingStatsResponse = {
  bar: string;
  line: string;
  punchcard: string;
  scatter: string;
};

export type FishingYearStatsResponse = {
  pie: string;
  bar: string;
  punchcard: string;
  scatter: string;
};
